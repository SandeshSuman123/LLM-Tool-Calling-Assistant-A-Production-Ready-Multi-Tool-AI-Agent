"""agent package — the core orchestration layer of the assistant.

Contains the LLM client wrapper, tool registry/executor, message handling,
streaming handler, and the conversation manager that ties them together.
"""
